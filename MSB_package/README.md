# MSB Package

The **MSB (Multimodality Stacking with Blockwise Missing Values)** package is designed for **survival analysis** in multimodal datasets with blockwise missing values. It integrates predictions from multiple data sources (modalities) while explicitly handling missingness, making it ideal for clinical datasets where missing values are common.

## Installation
```bash
pip install msb_package


